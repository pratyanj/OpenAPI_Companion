// ProjectService — detect project, derive stable ID, ensure default environment.
export { ProjectService } from './project-service'
export type { ProjectServiceOptions } from './project-service'
export {
  DEFAULT_ENVIRONMENT_ID,
  type ProjectInput,
  type ProjectMeta,
  type Environment,
} from './types'
